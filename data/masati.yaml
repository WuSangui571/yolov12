# MASATI dataset configuration for YOLOv12.
# Ultralytics resolves non-absolute paths under its internal datasets directory,
# so "../MASATI" points back to the repo root on both Windows and Linux.

path: ../MASATI
train: images/train
val: images/val
test: images/test

nc: 1
names:
  0: ship
